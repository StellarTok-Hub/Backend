# Root env for docker-compose orchestration.
# Each service also has its own .env.example for standalone local runs:
#   services/gateway/.env.example
#   services/analytics-service/.env.example

POSTGRES_USER=backend
POSTGRES_PASSWORD=backend
